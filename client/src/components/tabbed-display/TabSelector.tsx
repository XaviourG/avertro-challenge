import { ReactElement } from "react";

interface Props {
  tabTitles: string[],
  setFocusTab: (tabIndex: number) => void,
}

const TabSelector = ({
  tabTitles,
  setFocusTab
}: Props): ReactElement => {
  return <></>
}

export default TabSelector;
